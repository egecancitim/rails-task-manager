<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li> <%= link_to task.title, task_path(task) %> - <%= task.completed %> </li>
  <% end %>
</ul>

<p><%= link_to "Add New Task", new_task_path %></p>
